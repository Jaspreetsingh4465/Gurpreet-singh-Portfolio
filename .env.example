# Contact form delivery. Any URL that accepts a JSON POST of the form fields:
# a Formspree form endpoint, a Netlify/Vercel function, a small server.
# With this unset, the form validates fully and then reports that delivery is
# not connected. It never pretends to have sent anything.
VITE_CONTACT_ENDPOINT=
